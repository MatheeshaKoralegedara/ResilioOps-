apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: resilioops
  labels:
    app: backend

spec:
  type: NodePort

  selector:
    app: backend

  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 32466