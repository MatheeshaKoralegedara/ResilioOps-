spec:
  template:
    spec:
      containers:
        - name: backend
          env:
            - name: DB_HOST
              value: postgres
            - name: POSTGRES_DB
              value: resilioops_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: mk1234
            - name: REDIS_HOST
              value: redis
