alertmanager:
  config:
    global:
      resolve_timeout: 5m

    route:
      receiver: discord

    receivers:
      - name: discord
        webhook_configs:
          - url: "http://alertmanager-discord.monitoring.svc.cluster.local:9094"
            send_resolved: true